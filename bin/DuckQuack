#!/bin/sh
APPPATH=$(dirname $0)
BASEPATH=$(realpath $APPPATH)
CD=$(realpath $(dirname $BASEPATH))
cd $CD

java -Dfile.encoding=UTF8 -jar $BASEPATH/jruby-complete.jar -S ../app/duck_quack.rb DuckQuack
